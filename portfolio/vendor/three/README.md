# Three.js Vendor Copy

This folder vendors the browser ESM build from `three@0.165.0` so the site does not load Three.js from a third-party CDN at runtime.

Included files:

- `three.module.js` from the npm package's `build/` directory
- `LICENSE` from the npm package
- `package.json` for version/provenance reference

